# Custom Responsive Navbar #

A Bootstrap navigation bar that uses manual JS for mobile toggling.
Features

    Custom Theming: Uses CSS :root variables for easy color management.

    Lightweight Logic: Manual JS toggle function for the mobile menu.

    Responsive: Built with Bootstrap’s grid system for cross-device compatibility.
